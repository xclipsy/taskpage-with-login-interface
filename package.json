{
  "name": "admin-tasks-spa",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "json-server": "^1.0.0-beta.3"
  }
}
